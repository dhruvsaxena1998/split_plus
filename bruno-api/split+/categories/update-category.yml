info:
  name: update-category
  type: http
  seq: 5

http:
  method: PUT
  url: "{{base_url}}/groups/{{group_id}}/categories/{{category_id}}"
  headers:
    - name: Authorization
      value: "Bearer {{access_token}}"
      enabled: true
  body:
    type: json
    data: |-
      {
        "name": "Groceries & Food",
        "icon": "🍕",
        "color": "#FF5722"
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  # Update Category

  Update an existing expense category.

  ## Headers
  - `Authorization`: Bearer token (access token)

  ## URL Parameters
  - `group_id`: ID of the group
  - `category_id`: ID of the category to update

  ## Request Body
  - `name` (optional): New category name
  - `icon` (optional): New emoji icon
  - `color` (optional): New hex color code

  ## Status Codes
  - `200 OK`: Category updated successfully
  - `401 Unauthorized`: Not authenticated
  - `403 Forbidden`: Not a group member
  - `404 Not Found`: Category not found
